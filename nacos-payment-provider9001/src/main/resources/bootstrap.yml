server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  boot: #spring-boot-admin 客户端配置
    admin:
      client:
        url: http://localhost:7000
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.243.129:8848

management: # 打开客户端 Actuator 的监控
  endpoints:
    web:
      exposure:
        include: '*'
